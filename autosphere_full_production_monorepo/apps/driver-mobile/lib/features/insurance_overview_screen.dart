
import 'package:flutter/material.dart';

class InsuranceOverviewScreen extends StatelessWidget {
  const InsuranceOverviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Insurance Overview")),
      body: const Center(child: Text("Production Screen - insurance_overview")),
    );
  }
}
