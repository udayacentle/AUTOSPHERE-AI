
import 'package:flutter/material.dart';

class LiveRepairScreen extends StatelessWidget {
  const LiveRepairScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Live Repair")),
      body: const Center(child: Text("Production Screen - live_repair")),
    );
  }
}
