
import 'package:flutter/material.dart';

class ClaimUploadScreen extends StatelessWidget {
  const ClaimUploadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Claim Upload")),
      body: const Center(child: Text("Production Screen - claim_upload")),
    );
  }
}
