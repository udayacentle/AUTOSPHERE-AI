
import 'package:flutter/material.dart';

class PredictiveMaintenanceScreen extends StatelessWidget {
  const PredictiveMaintenanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Predictive Maintenance")),
      body: const Center(child: Text("Production Screen - predictive_maintenance")),
    );
  }
}
