
import 'package:flutter/material.dart';

class MobilityScoreScreen extends StatelessWidget {
  const MobilityScoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Mobility Score")),
      body: const Center(child: Text("Production Screen - mobility_score")),
    );
  }
}
