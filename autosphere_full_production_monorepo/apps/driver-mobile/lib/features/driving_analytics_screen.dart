
import 'package:flutter/material.dart';

class DrivingAnalyticsScreen extends StatelessWidget {
  const DrivingAnalyticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Driving Analytics")),
      body: const Center(child: Text("Production Screen - driving_analytics")),
    );
  }
}
